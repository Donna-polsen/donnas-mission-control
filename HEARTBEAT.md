# HEARTBEAT.md Template

1. Run `node scripts/heartbeat_check.js` to poll the Makat API for the latest lab chats and session updates.
2. Cross-reference any new or urgent messages with our `LEARNINGS.md` playbook.
3. If there is an actionable event (e.g., a lab flag, a supplier price drop, a missing PO), alert Omri with a summary and a recommended next step.
4. If nothing needs attention, stay quiet by replying exactly: `HEARTBEAT_OK`.
